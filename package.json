{
  "name": "canvasrun",
  "version": "1.0.0",
  "description": "像css一样写canvas",
  "main": "index.js",
  "scripts": {
    "build": "tsc && babel --presets es2015,stage-1 src/ --out-dir lib/",
    "watch-compile": "npm run build -- --watch",
    "watch": "npm run watch-compile",
    "test": "mocha test/*"
  },
  "author": "luoxue",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^2.0.4"
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-1": "^6.16.0",
    "chai": "^4.1.2"
  }
}
